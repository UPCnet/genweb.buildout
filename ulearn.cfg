[buildout]
parts =
    filestorage
    instance
    omelette
    themepreview
    test
    codeintel

extends =
    buildout.cfg
    sources.cfg
    http://dist.plone.org/release/4.3-latest/versions.cfg
    versions.cfg
versions = versions

developeggs += ${max:eggs}
               ${ulearn:eggs}
